# Release Notes

Generated release artifacts for EclipseStud Copilot v2.8.0:

- `EclipseStud_Copilot_FULL_PROJECT_v2.8.0/` — clean extracted package folder ready for load unpacked in Edge.
- `EclipseStud_Copilot_v2.8.0_loadable.zip` — clean archive of the same package.

Validation performed:

- `creator-copilot-service` test suite passes (11/11).
- Packaged extension contents verified against required files in `manifest.json`.
- Static docs site verified by local HTTP access to `docs/index.html`.

Notes:

- The package directory and zip archive were generated from the repository root.
- No secrets, logs, certs, or `node_modules` were included in the final package.
